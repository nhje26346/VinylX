@model IEnumerable<VinylX.Models.ReleaseInstance>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quality)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quality)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReleaseInstanceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReleaseInstanceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReleaseInstanceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
